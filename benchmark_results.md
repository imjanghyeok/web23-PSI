# Docker 최적화 벤치마크 결과 보고서

1GB 램 서버 환경에서의 안정성을 검증하기 위해 로컬 환경(기존)과 테스트 서버 환경(신규 최적화)의 빌드 및 실행 성능을 비교 측정했습니다.

## 1. 측정 결과 요약

| 항목 | 기존 (Local) | 신규 최적화 (Dev) | 개선율/변화 |
| :--- | :--- | :--- | :--- |
| **빌드 시간** | 17초 | 23초 | +6초 (안전한 배포 과정 추가) |
| **메모리 사용량** | **247.9 MiB** | **46.34 MiB** | **약 81% 감소 (대폭 개선)** |
| **안정성** | 낮음 (OOM 위험) | **높음 (메모리 제한 적용)** | 빌드/실행 시 서버 다운 방지 |

## 2. 상세 분석

### ✅ 메모리 최적화 (248MB -> 46MB)
가장 중요한 개선점입니다.
- **기존**: `nest start` 명령어를 사용하여 실행 시 개발 관련 오버헤드와 Typescript 컴파일러 등이 메모리를 많이 점유했습니다.
- **신규**: `node dist/main`으로 빌드된 순수 자바스크립트 파일만 실행하며, `pnpm deploy`를 통해 불필요한 의존성을 제거했기 때문에 초기 메모리 점유율이 극적으로 낮아졌습니다. 이는 1GB 서버에서 **여유 메모리를 약 200MB 이상 추가 확보**하는 효과가 있습니다.

### ⚠️ 빌드 시간 (17초 -> 23초)
신규 방식이 약 6초 더 소요됩니다.
- 이는 `pnpm deploy --prod` 명령어를 통해 **프로덕션용 패키지만 별도로 격리/설치**하는 과정이 추가되었기 때문입니다.
- 시간이 조금 더 걸리더라도, 이 과정 덕분에 **빌드 중 메모리 폭주를 막고** 최종 이미지를 경량화할 수 있으므로 저사양 서버에 필수적인 투자입니다.

### 🛠️ 적용된 기술적 변경 사항
1.  **pnpm deploy**: `devDependencies`를 완벽히 제외하고 실행에 필요한 파일만 이미지에 담았습니다. ( 호환성 문제를 위해 `--legacy` 플래그 적용)
2.  **수동 dist 복사**: `pnpm deploy`가 놓치는 빌드 아티팩트를 명시적으로 복사하여 실행 오류를 방지했습니다.
3.  **리소스 격리**: `Dockerfile.dev`와 `docker-compose.dev.yml`을 분리하여 로컬 개발 환경에 영향을 주지 않고 테스트 가능하도록 구성했습니다.

## 3. 결론
새로 생성된 `packages/backend/Dockerfile.dev` 설정은 **1GB 램 서버에서 운영하기에 매우 적합**합니다. 메모리 사용량이 1/5 수준으로 줄어들어, 다른 컨테이너(MySQL 등)와 함께 구동하더라도 시스템이 다운될 확률이 현저히 낮아졌습니다.
