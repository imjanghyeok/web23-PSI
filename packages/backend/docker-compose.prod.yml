services:
  backend:
    image: ${DOCKER_USERNAME}/web23-backend:${IMAGE_TAG}
    container_name: web23-backend-prod
    restart: always
    environment:
      - NODE_ENV=production
    ports:
      - '${PORT}:${PORT}'
    env_file:
      - .env
    depends_on:
      - mysql
    networks:
      - web23-network-prod

volumes:
  mysql_data_prod:

networks:
  web23-network-prod:
    driver: bridge
